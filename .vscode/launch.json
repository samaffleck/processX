{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "processX_app (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.buildDirectory}/processX_app",
      "cwd": "${command:cmake.buildDirectory}",
      "preLaunchTask": "CMake: build",
      "args": [],
      "console": "integratedTerminal"
    },
    {
      "name": "processX_tests (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${command:cmake.buildDirectory}/processX_tests",
      "cwd": "${command:cmake.buildDirectory}",
      "preLaunchTask": "CMake: build",
      "args": [],
      "console": "integratedTerminal"
    }
  ]
}
